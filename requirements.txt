# Core pipeline
requests>=2.31
feedparser>=6.0
PyYAML>=6.0
html2text>=2024.2.26

# Optional: only needed for the reference triage judge (pipeline/triage.py).
# Any model provider works; swap in your own judge function instead.
# anthropic>=0.39
